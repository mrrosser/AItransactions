import { describe, it, expect } from 'vitest'; import { ensureStores } from '../src/store/ensure'; import { saveReceipt, listReceipts } from '../src/store/receipts'; describe('persist',()=>{ it('saves & lists', async()=>{ await ensureStores(); saveReceipt('X402','SIMULATED',{ok:true}); const r=listReceipts(1); expect(r.length).toBeGreaterThan(0); expect(r[0].rail).toBe('X402') }) })
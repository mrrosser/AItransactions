# Example Streamlit secrets for local/testing environments.

# Core API endpoints
CONTROL_CENTER_URL = "http://localhost:8787"
API_BASE_URL = "http://localhost:8787"

# Optional feature toggles / credentials
FIRECRAWL_API_KEY = "fc-xxxx"
FIRECRAWL_API_URL = "https://api.firecrawl.dev"
FORTIFYY_DEMO_PASSWORD = "demo"

# Add client-specific keys below; keep this file out of production.
